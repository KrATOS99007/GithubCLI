spring:
  application:
    name: spring-boot

  datasource:
    url: jdbc:postgresql://localhost:5432/github_cli_db
    username: pguser
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true