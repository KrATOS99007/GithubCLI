services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: pguser
      POSTGRES_PASSWORD: 123456
      PGDATA: /data/pg
    ports:
      - "5432:5432"
    restart: unless-stopped